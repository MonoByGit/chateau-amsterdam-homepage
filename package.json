{
  "name": "chateau-homepage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "sirv dist --port $PORT --host"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "sirv-cli": "^3.0.1"
  }
}
